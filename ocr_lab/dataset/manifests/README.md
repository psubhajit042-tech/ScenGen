# Manifest Notes

- `rec_gt_seed.txt` is generated by `bootstrap_rec_dataset.py`
- review and correct that file before using it for training
- after correction, split it into train and validation files such as:
  - `rec_gt_train.txt`
  - `rec_gt_val.txt`

Expected PaddleOCR recognition manifest format:

```text
ocr_lab/dataset/rec_images/example_001.png\tSettings
ocr_lab/dataset/rec_images/example_002.png\tCompose
```
